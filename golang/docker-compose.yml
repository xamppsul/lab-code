version: "3.8"

services:
  app:
    build:
      context: ./
      dockerfile: dockerfile
    container_name: go-app
    ports:
      - "8089:8089"
    env_file:
      - .env
    restart: unless-stopped
    networks:
      - app

networks:
  app:
    driver: bridge